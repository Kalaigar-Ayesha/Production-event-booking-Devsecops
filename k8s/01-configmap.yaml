apiVersion: v1
kind: ConfigMap
metadata:
  name: eventora-config
  namespace: eventora
data:
  # Backend
  NODE_ENV: "production"
  API_PORT: "5000"

  # Frontend - must match Ingress routing
  VITE_API_BASE_URL: "/api"
